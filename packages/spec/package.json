{
  "name": "@m13/spec",
  "version": "0.3.0",
  "license": "MIT",
  "description": "m13 v0.3 modular overlay — education (kit) + game (V2). Headless Zod. Strip-to-visual. Zero renderer/compiler imports.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "fixtures",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/spec",
    "validate": "tsx src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.4.2",
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.3",
    "typescript": "^5.4.5"
  }
}
