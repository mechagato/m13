# m13 editor — environment variables (copia a .env.local)

# phi-llm-gateway URL — puerto 9095 default en dev
NEXT_PUBLIC_PHI_LLM_URL=http://localhost:9095

# Token Bearer para autenticar al gateway — phi-dev-local en dev
NEXT_PUBLIC_PHI_LLM_TOKEN=phi-dev-local
