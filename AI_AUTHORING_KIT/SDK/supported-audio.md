# Supported audio

`audio_reactive: true` or `{ band: bass|mid|treble }` on objects.

`events[].action.sound` is a catalog token: `success` `hint` `fail` (runtime maps to a clip). Do not embed URLs — the engine is local-first.
