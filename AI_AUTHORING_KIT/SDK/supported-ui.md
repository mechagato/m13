# Supported UI

`ui.hud`:
- `education` — objective, XP, time, missions, inventory, help, map
- `minimal` — objective + XP
- `none` — no chrome

Locale `es` default. Map draws objects and npc from ids. Do not author screen coordinates.
