# Supported animations

Legacy: `bob`, `rotate`, `pulse` with `speed` and `amplitude ≥ 0`.

Timeline v0.2+: `duration`, `loop`, `keyframes[{t, position?, rotation?, scale?, ease}]`.

Replay of student sessions is **not** authored in the file; see `GAMIFICATION` and the SDD.
