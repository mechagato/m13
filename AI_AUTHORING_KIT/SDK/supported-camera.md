# Supported camera

First person. `spawn` is the eye. `cameraSpeed` / `player.speed` in m/s.

There is no authored camera rig, FOV, or look-at target in v0.3. The player looks with the mouse. WASD strafes (FPS).
