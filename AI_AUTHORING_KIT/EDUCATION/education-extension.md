# Education extension (v0.3)

Additive. Geometry remains valid 0.2 if education keys are stripped.

Blocks: `education` `player` `ui` `missions` `npc` `zones` `quizzes` `score` `events` (extended).

Design rule: **one file = one lesson**. Do not span multiple classrooms unless the user asks for a campus (then use zones).
